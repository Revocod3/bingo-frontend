/**
 * Production environment configuration
 */
const productionConfig = {
  apiBaseUrl: 'https://api.yourdomain.com/api', // Replace with your actual production API URL
};

export default productionConfig;
